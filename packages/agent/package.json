{
	"name": "@agiaas/agent",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"server:standalone": "../../.venv/bin/uvicorn scripts.on_call.webhook_receiver:app --reload --port 8678",
		"install": "pnpm -w python:install",
		"check-types": "pyright scripts/"
	},
	"devDependencies": {
		"pyright": "^1.1"
	}
}
